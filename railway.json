{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && python3 -m pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend && python3 -m uvicorn app:app --host 0.0.0.0 --port 8000"
  },
  "nixpacks": {
    "python": "3.11"
  }
}
